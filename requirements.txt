pandas>=2.2.0
ruamel.yaml>=0.17.40
pyarrow>=15.0.0
# Optional for generating FlowBySector files locally (not required to run scripts)
# # FlowSA - pinned to specific version for reproducibility
# This version matches the reference parquet file (v2.0.3)
# Install with: pip install git+https://github.com/USEPA/flowsa.git@v2.0.3
# Or use the installation script: python install_flowsa_2.0.3.py
git+https://github.com/USEPA/flowsa.git@v2.0.3

# Data processing dependencies
pandas>=1.3.0
ruamel.yaml>=0.17.0
pyarrow>=6.0.0
openpyxl>=3.0.0
